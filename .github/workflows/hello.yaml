name: hello actions

on:
  schedule:
    - cron: '40 01 30 12 5'
    
  pull_request:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Random name
        run: echo "This?"
      - run: echo "Working?"
      - run: echo "One more?"
